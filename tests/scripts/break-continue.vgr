
# Test general and specific "break" and "continue" usage
# in all control loops. These are all positive tests.
# Assertions are placed in "dead code" locations to make
# sure the statements take the correct action.

Declare continued, broke

Define Function start_test(name):
    Print name
    Set continued To False
    Set broke To False
End-Function

Define Function end_test():
    Assert continued : "Test never hit Continue"
    Assert broke : "Test never hit Break"
End-Function

Call start_test("For Next (BASIC, block=For)")
For x = 1 To 3
    If $loop.first
        Set continued = True
        Continue-For
        Assert False: "Continue-For failed"
    Else
        Set broke = True
        Break-For
        Assert False: "Break-For failed"
    End-If
    Assert False: "Reached unexpected location"
Next
Call end_test()

Call start_test("For Each (Basic, block=for)")
For Each x In [1, 2, 3]
    If $loop.first
        Set continued = True
        Continue-For
        Assert False: "Continue-For failed"
    Else
        Set broke = True
        Break-For
        Assert False: "Break For failed"
    End-If
    Assert False: "Reached unexpected location"
Next
Call end_test()

Call start_test("For Next (VGR)")
For x = 1 To 3
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Break
        Assert False: "Break failed"
    End-If
    Assert False: "Reached unexpected location"
Next
Call end_test()

Call start_test("Perform Until (COBOL)")
Perform Until False
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Break
        Assert False: "Break failed"
    End-If
    Assert False: "Reached unexpected location"
End-Perform
Call end_test()

Call start_test("Perform Until (COBOL, block=Perform)")
Perform Until False
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Exit Perform
        Assert False: "Exit Perform failed"
    End-If
    Assert False: "Reached unexpected location"
End-Perform
Call end_test()

Call start_test("Perform Varying (COBOL)")
Perform Varying x From 1 By 1 Until x > 3
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Break
        Assert False: "Break failed"
    End-If
    Assert False: "Reached unexpected location"
End-Perform
Call end_test()

Call start_test("Perform Varying (COBOL, block=Perform)")
Perform Varying x From 1 By 1 Until x > 3
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Exit Perform
        Assert False: "Exit Perform failed"
    End-If
    Assert False: "Reached unexpected location"
End-Perform
Call end_test()

Call start_test("Repeat Times (VGR)")
Repeat 3 Times:
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Break
        Assert False: "Break failed"
    End-If
    Assert False: "Reached unexpected location"
End-Repeat
Call end_test()

Call start_test("Until (VGR)")
Until False
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Break
        Assert False: "Break failed"
    End-If
    Assert False: "Reached unexpected location"
End-Until
Call end_test()

Call start_test("While (VGR)")
While True
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Break
        Assert False: "Break failed"
    End-If
    Assert False: "Reached unexpected location"
End-While
Call end_test()

Call start_test("While (VGR, block=While)")
While True
    If $loop.first
        Set continued = True
        Continue-While
        Assert False: "Continue While failed"
    Else
        Set broke = True
        Break-While
        Assert False: "Break-While failed"
    End-If
    Assert False: "Reached unexpected location"
End-While
Call end_test()

Call start_test("For-Each (VGR)")
For-Each x In [1, 2, 3] : # keep single keyword form
    If $loop.first
        Set continued = True
        Continue
        Assert False: "Continue failed"
    Else
        Set broke = True
        Break
        Assert False: "Break failed"
    End-If
    Assert False: "Reached unexpected location"
End # keep untyped
Call end_test()

Call start_test("For-Each (VGR, block=for)")
For-Each x In [1, 2, 3]
    If $loop.first
        Set continued = True
        Continue-For
        Assert False: "Continue-For failed"
    Else
        Set broke = True
        Break-For
        Assert False: "Break-For failed"
    End-If
    Assert False: "Reached unexpected location"
End-For
Call end_test()
